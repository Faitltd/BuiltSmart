import { RoomType, DesignStyle, ProductCategory } from '../../types/conversation';

// Mock function for product search
export const searchHomeDepotProducts = (
  query: string = '',
  filters: any = {},
): any[] => {
  // This would be replaced with the actual implementation
  // For now, return an empty array
  return [];
};
